class Client(object):


        def __init__(self,client_name, ip, port, join_id,socket):
                self.Client_Name = client_name
                self.IP = ip
                self.Port = port
                self.Join_ID = join_id
                self.socket = socket



